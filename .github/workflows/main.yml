name: CI DevOps 2024
on:
  push:
    branches: 
      - main
      - dev
      - feature
  pull_request:

jobs:
  test-backend: 
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v2.5.0

      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Build and test with Maven
        run: mvn clean verify
        working-directory: simple-api

  build-and-push-docker-image:
    needs: test-backend
    # Run only when code is compiling and tests are passing
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout code
        uses: actions/checkout@v2.5.0

      - name: Build image and push backend
        uses: docker/build-push-action@v3
        with:
          # Relative path to the place where the source code with Dockerfile is located
          context: ./simple-api
          # Note: tags have to be all lowercase
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/tp-devops-simple-api:latest

      - name: Build image and push database
        # Placeholder: Add the configuration to build and push the database image
        run: echo "Build and push database image step not yet implemented."

      - name: Build image and push httpd
        # Placeholder: Add the configuration to build and push the httpd image
        run: echo "Build and push httpd image step not yet implemented."
